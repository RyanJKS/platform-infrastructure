# Production subscription placeholder

This folder reserves `PROD-JKS` for production workloads, separate from `DEV-JKS`
and the `PROD-HUB` hub subscription.

Before adding units, configure the production subscription identity and add the
`prod/<region>/<spoke>/` settings hierarchy. Connectivity to `PROD-HUB` must
be configured explicitly. No deployable configuration exists here yet.
